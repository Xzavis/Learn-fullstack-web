// Node.js Standart Libary untuk operasi system
import os from "os";

console.log(os.userInfo()); // untuk menampilkan informasi user
console.info(os.platform()); // untuk menampilkan platform sistem
console.table(os.cpus()); // untuk menampilkan informasi cpu